# 변경 이력 (CHANGELOG)

이 프로젝트의 주요 변경 사항을 기록합니다.

## [1.8.0] — 자동 진행(오토 호스트) 모드
- 사람 진행자 없이 게임을 자동 진행하는 **오토 호스트** 추가(`config.autoHost`, 기본 OFF):
  · 상태 머신: 낭독 → 응답 카운트다운 → 자동 정답 공개 → (4지선다) 자동 채점 → 감상 후 자동 다음
  · 진행 컨트롤 **▶/⏸/⏭** (방장/진행 화면용 보조 — 안 눌러도 진행), 마지막 문항 뒤 결과 탭 자동 이동
  · **진행 컨트롤 숨기기**(`autoHideControls`) 토글 → 버튼 없는 키오스크 모드
  · 정답 공개 후 다음까지 간격(`autoInterval`, 기본 7초) 조절
  · 브라우저 자동재생 정책상 첫 시작 탭 1회만 필요(그 뒤 자동)
- 회귀 테스트 추가(무클릭 자동 진행·일시정지·컨트롤 숨김) → Playwright 68항목 통과

## [1.7.0] — 팀 선택 가리기(비공개) 옵션
- 팀전 4지선다에 **팀 선택 가리기** 토글 추가(기본 ON):
  · **온라인·동시 진행**: 모든 팀이 고를 때까지 각 팀 선택을 비공개 → 먼저 고른 팀 선택이 노출되지 않음
  · **오프라인**(팀 논의 후 선택): 끄면 각 팀 선택이 즉시 배지로 표시(운영 방식에 따라 선택)
- 채점 시 전체 공개(팀별 정답/오답 + 정답 보기), `config.teamBlind` 저장·동기화
- 단일 화면 앱 특성상 개별 기기 실시간 동시입력(진짜 멀티플레이)은 미지원임을 문서에 명시
- 회귀 테스트 추가(가리기 ON 중간 비공개 / OFF 즉시 표시) → Playwright 63항목 통과

## [1.6.0] — 팀전 4지선다 팀별 순차 선택·자동 채점
- 팀전 + 4지선다에서 **각 팀이 차례대로 보기를 선택**하는 방식으로 변경(2팀 이상 고려):
  · `🎤 팀X 차례` 표시 + 선택 보기에 팀 배지, 모든 팀 선택 시 **자동 채점**(정답 팀마다 +1점, 타이머 시 시간 보너스)
  · 다음 문항은 **다음 팀부터** 시작해 순번이 공평하게 순환
- **먼저 맞히기**는 개인전/솔로에 한정(4지선다에서 선택 즉시 판정)
- 4지선다를 끈 팀전은 기존 **맞힌 팀 버튼** 수동 채점 유지
- 팀전 스크린샷 갱신, 팀전-4지선다 회귀 테스트 추가 → Playwright 61항목 통과

## [1.5.0] — 2025~2026 유닛 프로젝트 6곡 추가 → 총 56문항
- V8·D&S·CxM·HxW 유닛 앨범에서 공식 MV/영상 보유 곡 6곡 추가(웹서치 4중 병렬 조사):
  · V8(버논X디에잇) 「singasong」·「미아(mia, 버논 솔로)」·「girlsnboys」
  · D&S(도겸X승관) 「Blue」(소야곡 타이틀), CxM 「For you」, HxW 「STUPID IDIOT」
  · 각 곡 제목 미포함 클루 확보(한/영), MV ID는 HYBE LABELS/SEVENTEEN 공식 채널 검증
- "원정대"는 정한X원우(=「어젯밤」)의 팬 조합명으로 확인 → 이미 수록되어 추가 없음
- 8DM(중국어)·b사이드 오디오 온리 트랙·팬 리업로드는 제외
- 상 21 / 중 21 / 신곡 14, 신곡 정의 2024~2026으로 확장, svt-data.json rev 갱신

## [1.4.0] — 저장 슬롯 커밋 일원화 · 발매곡(영어) 3곡 추가
- **저장 슬롯 = 바로 커밋 가능** 구조로 변경(관리자 전용):
  · **+ 현재 상태 저장** 시 슬롯 보관과 동시에 `svt-data.json`을 커밋용으로 자동 다운로드
  · 각 슬롯의 **💾 배포본** 버튼으로 지난 구성도 `rev` 포함 `svt-data.json`으로 내려받기
- 발매곡 3곡 추가(사용자 요청, 영어곡 포함) → **총 50문항** (상 21 / 중 21 / 신곡 8):
  · 우지 「Ruby」(2022), 버논 「Black Eye」(2022), 쿱스X민규 「5, 4, 3 (Pretty woman)」(2025)
  · 파트에 `(영어)` 표기로 사회자가 낭독 특성을 인지하도록 안내

## [1.3.0] — 유닛·솔로곡 7곡 확장 (공식 MV 기준)
- 서브유닛·페어링·솔로 신규 7곡 추가 → **총 47문항** (상 19 / 중 21 / 신곡 7)
  · 부석순 「거침없이(Just do it)」, SVT LEADERS 「CHEERS」, 원우X민규 「Bittersweet」,
    호시 「Spider」, 디에잇 「나란히(Side By Side)」, 도겸 「You're My Christmas」, 호시X우지 「동갑내기(96ers)」
  · 웹서치 3중 병렬 조사로 **한국어 가사 + 공식 MV(HYBE LABELS 채널)** 확인, 제목 미포함 벌스/랩 클루 선정
  · 영어곡(Ruby·Black Eye·CxM 5,4,3)·MV 없는 곡(BSS Lunch)·비공식 리업로드는 제외
- 유닛 가중치 판정(`isUnitSong`)에 LEADERS·솔로·멤버 페어링(X) 인식 추가
- README·데이터 스펙 곡 목록·개수 갱신, svt-data.json rev 갱신(배포 동기화)

## [1.2.0] — 흐름 재구성 · 저장 일원화 · 자동 낭독 · 최종 데이터
- 랜딩 → 기본 설정(게임 옵션) 화면 → 게임 시작 흐름, 저장 기능 저장 슬롯으로 일원화
- 기본 설정 화면 옵션별 안내 문구(토스 톤), 문항 전환 시 2초 뒤 TTS 자동 낭독
- 최종 가사·타임스탬프·영상 링크 반영(40곡 확정), 타이머 20초·4지선다 기본 ON

## [1.1.0] — 문서 보강 · 곡 정리 · 음원 링크 확충
- 미확보 8곡(Trauma·Space·Shhh·박수·어쩌나·거침없이·Spell·Bad Influence) 제거 → **40곡 전곡 가사 입력**
- MV 없는 곡에 **공식 오디오 링크 보강** → 영상/음원 링크 34/40곡 (바람개비·청춘찬가는 공식 MV, Kidult·우리의 새벽·엄지척·Fire는 공식 오디오)
- README 전면 개편(스크린샷·목차·공유 방법·FAQ), 운영 가이드·데이터 스펙 문서 추가
- `docs/img/` 스크린샷 추가(게임·정답 공개·설정·팀전·결과)

## [1.0.0] — 최초 릴리스
- 단일 `index.html` 세븐틴 가사 퀴즈 앱(외부 의존성 0, localStorage 저장)
- 게임: 속도조절 TTS 낭독, 가사 가리기, 정답 공개 + MV 타임스탬프 재생, 수동 채점, 진행률·정답률
- 확장: 난이도 필터, 문항 셔플, 타이머(제한시간·시간보너스), 팀전 모드(점수판·턴 관리)
- 설정: 곡명·파트·난이도·가사·MV·시작시간 편집, 곡 추가/삭제, 일괄 입력, JSON 내보내기/불러오기
- 결과: 정답률·등급 부여, 난이도별 분해, 팀 순위
- 곡 데이터: 실제 가사 발췌를 웹서치로 확보 후 **다수 출처와 교차검증**, 공식 MV 링크·추정 타임스탬프 내장
