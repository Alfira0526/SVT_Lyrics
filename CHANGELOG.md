# 변경 이력 (CHANGELOG)

이 프로젝트의 주요 변경 사항을 기록합니다.

## [1.41.0] — 📊 S2 대시보드: 곡별 정답률 막대 시각화 + 이탈-only 표시
- **곡별 정답률 대시보드 시각화** — 관리자 리포트 곡별 표에 **정답률 막대(빨강↔초록)** 추가로 어려운 곡·쉬운 곡을 한눈에. 난이도 라벨도 `diffLabel`로 다국어.
- **완주 0(이탈만) 상태도 퍼널 표시** — 세션 0이어도 `funnel`에 시작·이탈이 있으면 리포트를 렌더(기존엔 "기록 없음"으로 숨겨지던 엣지 수정).
- 검증: 관리자 리포트 스크린샷(퍼널·요약·막대), S2 11항목·회귀 유지.

## [1.40.0] — 📊 S2 분석①: 이벤트 로깅·퍼널(완주율·이탈지점)·언어·랭킹 등록
- **퍼널/이벤트 로깅(로컬·익명)** — 기존 W2 통계(`svt_lyrics_quiz_stats_v1`)에 `funnel`·`langCounts`·`rankRegs` 추가(하위호환 자동 생성). 게임 **시작→완주→이탈**을 모드별로 집계.
  - `funnelStart()`(게임 시작·재시작 시), `funnelComplete()`(결과 도달=recordStats), `funnelAbandon()`(완주 전 홈 이동·탭 종료 `pagehide`). 미완 게임이 다음 시작으로 넘어가면 자동 이탈 처리(중복 방지).
  - **이탈 지점**: 이탈 시 진행률(pos/전체 %)을 누적해 모드별 평균 이탈지점 산출.
  - **언어 분포**: 게임 시작 시 현재 UI 언어(ko/en) 카운트. **랭킹 등록 수**: `rankRegister`에서 보드별 집계.
- **관리자 리포트 확장** — 상단 요약(시작·완주·**완주율**·이탈·랭킹등록·언어 분포) + **모드별 완주율·이탈지점 퍼널 표** 신설(기존 모드별 정답률·곡별 정답률 표 위). JSON 내보내기에 신규 필드 자동 포함.
- 검증: Playwright 11항목 — 시작/완주/이탈/재시작 카운트, 이탈지점 기록, 언어·랭킹등록 집계, 리포트 퍼널표 렌더. 방·공유 회귀 전부 통과.

## [1.39.0] — 🐛 트위터 공유에 결과 이미지 첨부 + 공유 문구/이미지 다국어
- **[BUG] 트위터 공유 시 이미지 미전송 수정:** 기존엔 X 웹 인텐트 URL만 열어(플랫폼 특성상 이미지 첨부 불가) **문구만** 올라갔음. 이제 **모바일은 Web Share API로 결과 이미지(PNG)를 첨부한 공유 시트**를 띄워 X를 고르면 이미지가 함께 올라감. **데스크톱은** 인텐트로 문구를 자동 입력하고 **결과 이미지를 저장**해 수동 첨부하도록 안내(웹 인텐트는 이미지 불가).
- 공통 헬퍼 `makeResultBlob()`·`shareImageNative()`·`downloadImageBlob()`로 트위터·인스타 공유 경로 통일.
- **공유 문구·결과 이미지 다국어** — `shareCaption()`·결과 카드 이미지(제목·정답률·태그라인)·카카오/복사 토스트 ko/en.
- 검증: Playwright 8항목 — 블롭 생성, 모바일 파일첨부 공유, 데스크톱 인텐트+이미지 저장, ko/en 문구.

## [1.38.0] — 🌍 S1-e: 세부 설정 화면까지 영어화 (앱 전면 다국어 완성)
- **세부/고급 설정 화면 ko/en** — 제목·시나리오 카드(4종)·팀 수·듣기 전용·문제 수·문제 범위(난이도/구성)·모드 카드·제한시간·효과음·팀전·4지선다/가리기/문제만·자동 진행·온라인 방·Firebase 주소·음성(TTS) 확인까지 라벨·긴 설명(setup-tip) 전부 다국어.
- JS 동적부: `renderScenarios`·`renderScopeSetup`(난이도=diffLabel·구성=`scopeValLabel`)·`renderVoiceCheck`(언어명·상태·설치안내) 다국어. `data-i18n-ph`로 Firebase 주소 placeholder까지.
- 이제 **랜딩·팝업·가이드·게임·방/참가자·결과·랭킹·업데이트·세부설정** 전 화면 영어 플레이 가능. (관리자 분석 리포트만 내부용 한국어 유지)
- 검증: 키 파리티(ko/en 동수), 영어 설정 스크린샷(시나리오·범위·타이머·팁), 방 회귀 11×2 통과.

## [1.37.0] — 🌍 S1 마무리: 업데이트 팝업·핵심 토스트 영어화 (전 플로우 영어 플레이 가능)
- **업데이트 알림 팝업 ko/en** — 데이터 주도(`title_en`/`items_en`) 이중언어. 첫 공지에 "🌍 영어 지원 시작" 항목 추가.
- **핵심 플레이 토스트/확인창 24종 ko/en** — 정답/오답·시간종료·스킵경고·서든데스·게임종료/홈/재시작 확인·모드전환·새문제·영상재로드·팀랜덤·TTS 미지원·공유(트위터/인스타/복사) 등.
- ✅ **S1 완료**: 랜딩 → 모드 시작 → 게임 방법 → 게임 화면 → 결과 → 랭킹 → 업데이트 팝업까지 **전 플로우 영어로 플레이 가능**. 언어 토글/`?lang=`/자동감지 지속.
- 검증: 키 파리티(ko/en 동수·DOM 전키 존재), 영어 랜딩/가이드/게임/결과/랭킹/업데이트 스크린샷, 방 2페이지 회귀 11×2 통과, 무회귀.
- ℹ️ 후속(S1-e, 저우선): **세부 설정 편집기**(곡·팀·범위 편집)와 **관리자 분석 리포트**는 고급/관리자 전용 화면으로 한국어 유지 — 일반 플레이 경로엔 영향 없음.

## [1.36.0] — 🌍 S1: 결과·랭킹 화면 영어화
- **결과 화면 ko/en** — 팀전 결과·채점 결과·등급(6단계 emoji+title+desc)·정답률/진행/전체·난이도별 정답·놓친 곡 리캡·개인 최고기록·랭킹 등록 안내·공유 버튼·이어가기 안내까지 전부 다국어.
- **랭킹 팝업 ko/en** — 제목·탭(혼자/무한)·스코프(전 세계/이 기기)·빈 목록·불러오는 중·행 값(N곡·N초·연속)·닉네임 입력·등록·완료 문구, 인트로 실시간 랭킹 미리보기까지.
- **무한 게임오버 오버레이 ko/en** — 최고기록 경신/게임 오버·연속·최고기록·정답·다시도전/그만두기.
- `renderRankRows`도 다국어(위→#n, 점→pt) — 팀 결과·최종 순위 공용.
- 검증: 키 파리티(ko/en 동수·DOM 전키 존재), 영어 결과/랭킹 스크린샷.
- ⏭️ 다음(S1-d): 업데이트 팝업 → 전체 통합 검증.

## [1.35.0] — 🌍 S1: 게임 화면·방 로비·참가자 화면 영어화
- **게임 화면 전체 ko/en** — 헤더·탭(게임/설정/결과)·카운터(문항/정답/오답)·난이도 뱃지(상·중·신곡→Hard·Med·New)·타이머 단위(초→s)·속도·가리기 토글·힌트·낭독/공개/채점 버튼·자동 진행 바·범위 표시·필터 칩까지 `data-i18n`+`i18nT()`로 전환.
- **온라인 방(방장)** — 방 코드 바·참가 현황·팀 선택(호스트 참여)·팀 순서/부저 판정 패널 문구 다국어.
- **참가자(폰) 화면** — 방 참가·팀 선택·대기/문항/제출/공개 상태·부저 버튼·최종 순위까지 전부 다국어.
- 프레임 확장: `data-i18n-ph`(placeholder) 지원 추가, `diffLabel()`·`teamLabel()` 헬퍼로 데이터 값(난이도·팀명) 표시만 번역(로직 키는 유지).
- 검증: 키 파리티 자동검사(ko 176 = en 176, DOM 106키 전부 존재), 영어 게임/가이드 화면 스크린샷, 방 2페이지 회귀 11×2 통과.
- ⏭️ 다음: 결과·랭킹 화면 → 업데이트 팝업. (세부 설정 편집기는 고급 화면 — 후속.)

## [1.34.0] — 🐛 참가자도 결과 화면으로 이동 + 🌍 게임 방법 영어화
- **[BUG] 게임 종료 시 참가자 미이동 수정:** 방장이 '게임 종료'를 누르면 방장은 결과 화면으로 갔지만 **참가자(폰)는 방 화면에 그대로 남아** ▶ 듣기 버튼·검은 영상·범위 라벨이 잔류하던 문제. 이제 참가자도 **'최종 결과' 화면**으로 전환 — 플레이 컨트롤(듣기·가사·힌트·보기·영상)을 모두 감추고 **메달 순위표**를 크게 노출, **우리 팀은 강조 표시**. (원인: `rg-toprow`에 id가 없어 숨김 대상에서 누락 → id 부여 + `guestShowFinal()`·`guestResetPlayUI()` 신설)
- **🌍 S1: 게임 방법 가이드 팝업 영어화** — 제목·리드·4개 박스(방법·모드·친구랑·점수)·팁·옵트아웃·확인 버튼을 `data-i18n(-html)`로 ko/en. 참가자 최종 결과 문구도 ko/en.
- 검증: Playwright 2페이지(호스트+게스트 BroadcastChannel) 11항목 — 종료 시 게스트 최종순위 노출·컨트롤 숨김·우리 팀 강조, 호스트 결과 이동. 게임 방법 en 렌더 확인.

## [1.33.0] — 온라인 4지선다: 자동모드 기본 + 호스트도 팀으로 참여
- **자동모드 기본:** 온라인 4지선다는 이미 시작 시 `config.autoHost=true`로 진행(정답 공개 후 자동으로 다음 문항) — 재확인·검증.
- **호스트 참여:** 방장이 **직접 한 팀을 골라 게임에 참여**할 수 있음. 방 로비에 "🖥 나도 참가(호스트)" 드롭다운(`#rl-self-team`) 추가 → 팀 선택 시 가상 기기(`__host__`)로 해당 팀 슬롯을 점유하고 명단에 "(나)" 표시. 이미 참가자가 쓰는 팀은 비활성.
- 게임 중 방장 화면에 **본인 팀용 4지선다 보기(g-mc)** 노출 → 방장도 폰 없이 답을 제출(`host.answers[문항][내팀]`), 제출 완료 시 버튼 잠금·표시. 방장 답도 전원 제출 집계·자동 공개에 포함.
- 신설: `lobbyHostSelf()`·`hostJoinSelf()`·`hostAnswered()`·`hostSelfAnswer()`, `host.selfTeam`.
- 검증: Playwright 11항목 전부 통과 — 자동모드 기본, 로비 드롭다운·팀 선택→명단 반영, 진행 중 g-mc 제출 기록.

## [1.32.1] — 랜덤 팀 이름 풀 확장 (세븐틴 특화 9개 카테고리)
`TEAM_NAME_POOL`을 30개 → **70개**로 확장. 카테고리: 멤버 이름(13) · **미니틴 공식 캐릭터**(13, 웹검증) · 공식 유닛/연령라인·조합 · 별명·상징 동물 · **고잉세븐틴 코너**(논리나잇·노룰스·고잉코멘터리) · 노래/수록곡 유명 제목. 중복 0. (미니틴 캐릭터명·고잉세븐틴 코너명은 나무위키/뉴스로 사실 확인)

## [1.32.0] — 온라인 팀 대결: 팀 수 미리 설정 + 팀 이름 자동 랜덤
- **온라인 4지선다/부저의 '팀 대항'** 선택 시 모드 시작 팝업에 **팀 수 스텝퍼(2~8)** 노출 → 시작 전에 팀 수를 미리 지정. '개인전' 선택 시 자동으로 숨김.
- **팀 이름 자동 랜덤:** 시작 시 `TEAM_NAME_POOL`에서 랜덤으로 팀 이름을 부여(예: "아주 나이스"·"날쏘가즈"·"95즈"). 헬퍼 `randomizeTeamNames()` 신설.
- 검증: Playwright — 팀 수 스텝퍼 노출/토글·팀 3 반영·랜덤 이름·방 로비 진입·개인전 teamMode off.

## [1.31.1] — 혼자 모드 4지선다 고정 (주관식 불가)
랭킹(20문제 최속) 공정성을 위해 **혼자 모드는 무한과 마찬가지로 4지선다로 고정**.
- 모드 시작 팝업의 솔로 폼에서 **'문제 형식(4지선다/주관식)' 선택 제거** → 타이머·가사만.
- `startGame`에서 **혼자·무한(=팀전/온라인 아님)은 `config.mc=true` 강제** → 세부 설정에서 4지선다를 꺼도 시작 시 4지선다로 진행(주관식 자가채점으로 랭킹 조작 방지).
- 검증: Playwright — 솔로 폼 형식필드 없음, 팝업/세부설정 양 경로 mc 강제·보기 4개 렌더.

## [1.31.0] — 🐛 핫픽스: MV 폴백 링크 + 혼자 주관식 채점·시간 기록
QA 중 발견된 2건 수정.
- **MV 영상 안 나옴 → YouTube 바로가기 링크:** 자동재생 차단·임베드 비허용 등으로 영상이 검게 나오는 경우를 대비해 정답 공개 시 **▶ YouTube에서 보기** 링크를 항상 노출(`watch?v=…&t=start`). 임베드에 `playsinline=1` 추가.
- **혼자 주관식 채점 버그:** `mark()`의 `answerShown()` 가드가 **정답 공개 후 채점을 막던** 문제(주관식은 공개→채점 흐름) → 가드를 `marks[문항]` 기준으로 변경(첫 채점 허용, 재채점만 차단). 또한 주관식 경로에도 **소요시간(soloEndMs) 기록**을 추가해 20문제 완주 시 '최속' 랭킹이 정상 집계(기존엔 0초로 등록되던 버그).
- 검증: Playwright — 공개 후 채점 허용, 폴백 링크 href/노출, 주관식 20완주 "20곡 · N초" 랭킹 등록 확인.

## [1.30.1] — 🌍 S1 계속: 모드 시작 팝업 영어화
- **모드 시작 팝업 다국어화** — 정적 카드(제목·모드명·설명·버튼)는 `data-i18n`, JS 생성 폼(모드별 제목/설명·필드 라벨·옵션·안내)은 `i18nT()`로 ko/en. 언어 토글/자동감지에 즉시 반응.
- I18N 사전에 `ms_*` 키 35개 추가(ko/en).
- 검증: Playwright — en 로케일에서 카드/폼 영어, 바로 시작 게임 진입, ko 로케일 회귀 정상.
- ⏭️ 다음: 게임 방법 → 게임 화면(보기·컨트롤·카운터) → 결과/랭킹 → 업데이트 팝업.

## [1.30.0] — 🌍 글로벌 S1 착수: i18n 프레임워크 + 랜딩 영어
로드맵 S1(글로벌①) 첫 슬라이스. 국제 캐럿을 위한 다국어 기반을 깔고 랜딩부터 영어화.
- **i18n 프레임워크:** `I18N` 사전(ko/en) + `data-i18n`/`data-i18n-html` 속성 치환(`applyI18n`), `i18nT(key)` 헬퍼.
- **언어 자동 감지·전환:** `?lang=` → `localStorage(svtq_lang)` → 브라우저 언어 순. 랜딩에 **🌐 English/한국어 토글**(지속).
- **랜딩 영어화:** 제목·태그라인·기능 뱃지·버튼·고지 문구 전부 ko/en.
- 검증: Playwright — ko 로케일→한국어, en 로케일→영어 자동, 토글 양방향, `?lang=en`, 기존 게임 시작 스모크 전부 통과.
- ⏭️ **다음 슬라이스(S1 계속):** 모드 시작 팝업 → 게임 방법 → 게임 화면 → 결과/랭킹 순차 영어화. (각 화면에 `data-i18n` 태깅 + 사전 확장)

## [1.29.1] — 🐛 핫픽스: 모드 팝업 '바로 시작' 후 게임 화면이 안 뜸
- **증상**: 모드 시작 팝업에서 모드를 고르고 `바로 시작`을 눌러도 게임 화면으로 넘어가지 않음(랜딩 화면이 그대로 남음).
- **원인**: 정상 흐름의 `showSetup()`이 랜딩(#intro)을 `hide` 처리하는데, 팝업 흐름(`applyMsAndStart`)이 그 단계를 건너뛰어 게임 화면이 랜딩 뒤에 가려짐.
- **수정**: `applyMsAndStart`에서 시작 전에 `intro.classList.add("hide")` 추가(정상 흐름과 동일). 솔로/온라인 모두 정상 전환 확인(Playwright: 랜딩 숨김 + 게임 화면/방 로비 노출).

## [1.29.0] — 업데이트 알림 팝업 + 모드 팝업 팀전 카드 제거(회귀 수정)
- **모드 시작 팝업에서 '팀전(오프라인)' 카드 제거:** 앞서 제거 결정(팀대결·무대 시나리오)했던 오프라인 팀전이 QA 권고로 P6 팝업에 재유입된 것을 바로잡음. 팝업은 시나리오와 동일하게 **혼자·무한·온라인 4지선다·온라인 부저 4종**. (팀전 기능 자체는 세부 설정에 유지)
- **업데이트 알림 팝업 신설(로드맵 S0→S1 착수):** 재방문 사용자에게 신곡·새 기능을 "🎉 새로운 소식" 팝업으로 능동 공지. `UPDATES` 배열에 항목만 추가하는 **데이터 주도** 방식(버전 게이팅 `svtq_seen_update`). 첫 방문자에겐 미노출(게임 방법 우선), 랜딩 `📢 새 소식`으로 상시 수동 열람.
- 검증: Playwright 6항목(팝업 4모드·첫방문 미노출·재방문 자동노출·항목 렌더·seen 기록·수동 열람) 통과.

## [1.28.0] — 접근성 '글자 크게' 독립 옵션 + 솔로 스킵 처리 (QA 마무리)
- **🔎 글자 크게(접근성):** 무대 모드와 **별개**의 게임 화면 토글. 가사·보기·카운터·정답 곡명을 키우되 **힌트·속도·가사 가리기 컨트롤은 그대로 유지**(무대 모드는 이 컨트롤을 숨김) → 저시력 사용자가 확대와 기능을 동시에. `config.a11yBig` 지속.
- **솔로 스킵 = 오답 처리:** 보기를 안 고르고 `다음 →`을 눌러 넘기면 **오답으로 자동 집계 + 안내 토스트**(타임아웃 정책과 일관). 덕분에 솔로는 항상 20문제를 완주해 랭킹 등록이 유지됨. 무한에서 스킵은 '포기'로 간주해 게임 종료(연속기록 등록). 온라인/팀전은 관여하지 않음.
- 검증: Playwright — 글자 크게 시 컨트롤 유지(무대와 대조)·솔로 스킵 오답+진행 확인.

## [1.27.0] — 무한 모드 UX + 모바일 게임오버 (QA 후속 2차)
- **무한 화면 정리:** 서바이벌인데 '문항 N/총량'·진행바가 유한 퀴즈처럼 보이던 혼동 제거 → **∞ 표기 + 진행바 숨김**(연속 정답은 🔥 배지로 표시).
- **무한 자발적 종료 기록:** ⏹ 게임 종료를 누르면 결과 화면으로 빠지지 않고 **지금까지의 연속기록으로 게임오버 처리**(랭킹 등록 가능). 확인창에 현재 연속 수 표시.
- **무한 타이머 상속 방지:** `MODE_DEFAULTS.infinite`에 `timerOn:false` 명시 → 직전 모드의 타이머가 무한에 딸려와 '자기도 모르게 시간초과 즉사'하던 문제 차단.
- **모바일 게임오버 오버레이:** `io-overlay`에 `overflow-y:auto`, 짧은 화면(소프트키보드)에서 상단 정렬, `io-card` `max-height:92vh`+스크롤 → 닉네임 입력 시 등록/재도전 버튼이 가려지지 않도록.
- 검증: Playwright 3항목(∞·진행바 숨김·오버레이 스크롤·자발종료 게임오버) 통과.

## [1.26.0] — 모드 시작 팝업 (원터치 + 필수 결정만)
`시작하기`를 누르면 큰 설정 페이지 대신 **모드 선택 → 그 모드에 꼭 필요한 것만 → 바로 시작** 흐름을 제공(QA popupSpec 기반).
- **모드 선택 팝업:** 🎧 혼자 / ♾️ 무한 / ⚔️ 팀전(오프라인) / 📱 온라인 4지선다 / 🖐 온라인 부저. (오프라인 팀전 원터치 진입 신설 — 기존엔 세부설정을 열어야 했음)
- **모드별 필수 항목만:** 혼자=문제형식·타이머·가사, 무한=타이머·가사(4지선다 고정), 팀전=팀 수·채점방식·타이머, 온라인=참가 구성(팀/개인)+방 자동 개설. 각 항목은 **추천 기본값**으로 미리 선택돼 있어 그대로 `▶ 바로 시작` 가능.
- **세부 설정 유지:** 팝업의 `⚙️ 세부 설정으로`로 기존 전체 설정 화면에 접근(모든 기능 보존). 온라인 선택 시 방 로비로 정상 연결.
- 검증: Playwright 6항목(팝업 5모드·필수폼·솔로 20문제 바로시작·팀전 팀수 반영·세부설정 escape·온라인 방 로비) 통과.

## [1.25.0] — QA 후속: 타임아웃 정책 · 4지선다 단축키 · 접근성 라이브영역
페르소나 QA의 medium/high 개선점 반영(1차).
- **타임아웃 정책 확정:** 솔로·오프라인 팀전에서 제한시간 0초 시 **자동 오답 처리 + 정답 공개 후 진행**(솔로는 잠시 뒤 자동으로 다음 문항). 기존 '소리만 나고 방치'되던 상태 제거.
- **4지선다 단축키:** 게임 화면에서 **A/B/C/D 또는 1~4** 키로 보기 선택(데스크톱 최속 플레이). 입력창 포커스·정답공개 후·조합키는 무시.
- **접근성 라이브 영역:** #toast에 role=status·aria-live, 정답 카드(#g-answer)에 aria-live 부여 → 스크린리더가 정답/오답·공개 곡명을 자동 안내.
- 검증: Playwright 4항목(타임아웃 자동오답·단축키 선택·aria 속성) 통과.

## [1.24.0] — QA 기반 무결성 수정 (랭킹·점수 치트 경로 차단)
페르소나 QA(5모드×3인)에서 발견된 채점 무결성 결함 중 high 3건을 우선 수정.
- **솔로 정답공개 악용 차단:** `정답 공개`로 곡명을 본 뒤 그 보기를 눌러 20/20 최속을 조작하던 경로 제거. `pickMc`에 정답공개(answerShown) 가드 추가 + `showAnswer`가 모든 4지선다 보기를 비활성화.
- **무한 주관식 오답 종료 통일:** 4지선다를 끈 무한 모드에서 오답을 눌러도 종료되지 않던 버그 수정 — `mark()`에서 무한+오답이면 즉시 게임오버(연속기록 등록 경로 정상화).
- **팀전 재채점 중복 가산 방지:** 이미 채점된 문항을 재방문해 다시 채점하면 팀 점수가 중복 가산되던 문제를 `scoredQ`(문항별 채점 여부)로 차단(finalizeTeamRound·awardTeam).
- 검증: Playwright 3항목(정답공개 후 선택 차단·무한 주관식 게임오버·팀 점수 [1,0] 유지) 통과.

## [1.23.0] — TTS 다국어 전환 로딩 완화
다국어 가사 낭독에서 불필요한 음성 전환을 줄여 전환 지연을 완화.
- **짧은 이질 언어 흡수:** `oh·yeah·baby` 같은 공백 없는 짧은(≤4자) 비한국어 추임새는 인접 한국어 음성으로 읽어 전환(=로딩) 횟수를 줄임. 긴 영어 구절(공백 포함·5자↑)은 영어 음성 그대로 유지.
- 효과(측정): "난 oh 너를 baby 사랑해" 5→1 세그먼트, "떨리는 지금 yeah…" 3→1. 순수 영어 "Rock with you tonight"은 en 유지, "Shining Diamond 빛나는"은 en>ko 유지.
- 검증: Playwright로 세그먼트 감소·언어 유지 4케이스 확인.

## [1.22.1] — 솔로 랭킹 완화: 만점 아니어도 등록 (정답 수↑ · 동점 최속)
'20/20 만점'이라는 진입 장벽을 낮춤. **20문제를 끝까지 풀면**(정답 1개 이상) 누구나 등록.
- **정렬 규칙:** 1순위 **정답 수 많은 순**, 정답 수가 같으면 2순위 **총 소요시간 짧은 순**. (`solo_speed` 엔트리에 `t`(초) 필드 추가)
- **결과 카드:** "이번 기록 18곡 · 42초 · 랭킹 등록"처럼 정답 수+시간 함께 표기. 20문제를 끝까지 풀지 않으면 "끝까지 풀면 등록" 안내.
- **랭킹 UI:** 행 표기 "20곡 · 37초", 솔로 탭 "🎧 혼자 (20문제 · 정답↑·최속)". 보드별 복합 정렬(정답↓·시간↑) 반영.
- 검증: Playwright — 20/20·18/20 모두 등록, `t` 저장, 정답 수 내림차순 정렬 확인.

## [1.22.0] — 모드별 문제 수 + 솔로 '최속' 랭킹 재설계
개인/팀전의 문제 수 정책을 분리하고, 솔로 랭킹을 '20문제 만점 최속' 경쟁으로 전환.
- **문제 수 정책:** 개인(솔로)=**20문제 고정**, 팀전·온라인=문제 수 조절 가능, 무한=무제한. 설정의 `문제 수` 카드는 팀전·온라인에서만 노출.
- **솔로 랭킹 재설계:** 20/20 **만점** 시에만 등록 가능하며, **총 소요시간(게임 시작~마지막 정답)** 이 짧을수록 상위. 새 보드 `solo_speed`(초·오름차순)로 **교체**(기존 정답수 보드는 미사용). 만점이 아니면 결과에 "20문제 만점이면 최속 랭킹 도전" 안내.
- **무한 랭킹:** 틀리기 전 연속 정답 수 기준(기존과 동일, 많을수록 상위).
- **랭킹 UI:** 모달 솔로 탭 "🎧 혼자 (20문제 최속)", 인트로 미리보기 "🎧 혼자 · 20문제 최속(초)". 보드별 정렬 방향(오름/내림) 지원.
- 게임 방법 점수 박스도 신 랭킹 규칙으로 갱신(`GUIDE_VERSION` 2026-07-31 → 1회 재노출).
- 검증: Playwright 12항목(문제수 카드 노출·솔로 20 고정·20/20 만점 등록·solo_speed 오름차순 저장·모달 탭/단위) 전부 통과.

## [1.21.1] — 점수 안내 정정: 시간 보너스 없음 (1개 = 1점)
실제 점수 로직과 안내 문구를 일치시킴. 코드에는 시간 보너스가 없는데(혼자=정답 수, 무한=연속 수, 팀전=1문제 1점 고정) UI 3곳이 "시간 보너스"라고 잘못 안내하던 것을 바로잡음.
- **게임 방법 점수 박스:** "빨리 맞히면 보너스" 문구 삭제 → "정답 1개 = 1점 · 혼자는 맞힌 곡 수, 무한은 연속으로 맞힌 수"로 정정.
- **설정 팀전 라벨/팁:** "타이머 시 시간 보너스"·"빨리 맞힐수록 보너스" → "팀별 점수 대결 · 1문제 = 1점"으로 정정.
- 게임 방법 내용 변경 반영으로 `GUIDE_VERSION`을 올려(2026-07-30b) 기존 사용자에게 1회 재노출.

## [1.21.0] — 게임 방법: 초등학생용 리라이트 + 최초 자동 노출
처음 오는 사람도 바로 이해하도록 게임 방법을 다듬고, 첫 플레이 때 자동으로 안내합니다.
- **초등학생용 리라이트 + 박스 UI:** 어려운 말(낭독·구두·판정·서바이벌 등)을 쉬운 문장으로 교체. `🎯 이렇게 해요`(번호 3스텝)·`🎮 놀이 방법 4가지`·`👫 친구랑 같이 하려면`·`⭐ 점수는요` 박스와 💡 팁 콜아웃으로 가독성↑.
- **최초 자동 노출:** 페이지 로드가 아니라 **첫 `시작하기` 클릭 시** 게임 방법을 1회 자동으로 띄움(첫인상 화면은 그대로 유지).
- **재노출 정책(페르소나 토의 결론):** 이후엔 **가이드 내용 버전(`GUIDE_VERSION`) 상승** 또는 **마지막 열람 후 14일 경과** 시에만 1회 재노출. 랜딩 `❓ 게임 방법` 버튼은 상시 수동 열람.
- **옵트아웃:** 팝업 하단 "이제 안 볼게요" 체크 시 시간(14일) 재노출은 끄되, 새 기능(버전)은 계속 안내(`svtq_guide_noauto`).
- 검증: Playwright로 최초 노출/열람기록/알겠어요→설정/재방문 미노출/수동 버튼/옵트아웃 저장 6항목 통과.

## [1.20.0] — 테스트 사이트: 추가한 문제만 검수 (테스트 문제 전용 보기)
스테이징에서 **새로 추가한 곡만** 골라 검수하고, 승격되면 목록에서 빠지는 흐름 완성.
- **`_test` 후보 목록:** 스테이징 데이터 최상위 `_test`(테스트용 곡 id 배열). 앱 전역 `testIds`로 로드/저장/스냅샷/불러오기에 반영(프로덕션엔 실리지 않음).
- **🧪 테스트 문제만 버튼:** `?staging=1` 게임 화면에 노출(후보 곡이 있을 때만). 클릭 시 `_test` 곡만 출제(`scope.by="test"`). 후보가 있으면 **부팅 시 자동으로 테스트 문제만** 시작 → 전체 56곡 안 돌고 추가분만 빠르게 검수. 전체/난이도/구성 칩으로 즉시 복귀.
- **승격 시 자동 제외:** `promote.mjs`가 승격 성공 후 스테이징의 `_test`·`_review`를 비움 → 반영 완료된 곡은 테스트 목록에서 빠짐.
- **`tools/add-song.mjs`:** 곡 추가 헬퍼(한 줄 명령으로 `songs`·`content`·`album` 추가 + `_test`·`_review` 등록).
- 흐름: 문제추가(add-song) → `?staging=1` 테스트 문제만 검수 → 확인 → `promote` 실시간 반영 → 테스트 목록에서 제외.
- 검증: 신규 `testonly`(부팅 자동적용·버튼 노출/토글·3곡 제한·프로덕션 미노출) + add-song→validate→promote 왕복(스테이징 정리 확인) + 회귀 base 69·scope 13·w1~w4 전부 유지.

## [1.19.0] — 데이터 파이프라인(스테이징→검수→승격) + 시대축 폐기
지속적 곡 데이터 추가를 위한 구조 신설.
- **시대(연도) 그룹 축 폐기:** '전성기 지남' 뉘앙스를 유발하던 연도 프레이밍 제거. 문제 범위는 **난이도·구성**만.
- **데이터 정규화:** 힌트 메타(발매연도·앨범종류 = 기존 `ALBUM_INFO`)를 `svt-data.json`의 `album` 키로 편입. 앱은 `data.album` 우선 + 인라인 폴백(`albumOf`). → **곡 추가 시 JSON 한 파일만** 편집.
- **🧪 스테이징(검수) 사이트 `?staging=1`:** 같은 Pages가 검수용 `svt-data.staging.json`을 로드(상단 배너·저장키 분리로 실서비스 무오염). 공유 URL `…/SVT_Lyrics/?staging=1`.
- **검증·승격 툴링(`tools/`, 순수 Node):** `validate.mjs`(스키마·무결성 린트 + 검수요청 수집), `promote.mjs`(검증 통과 시 프로덕션 승격, rev 안전증가, 오류/미해결 검수요청은 차단).
- **검수요청(`_review`) + 확인 루프:** 불확실 데이터는 `_review`로 표식 → 오너 확인 전 승격 차단. 기존 보류분 **s63 Blue·s64 For you**를 검수요청으로 이관.
- 문서: `docs/DATA_PIPELINE.md` 신설, `DATA_SPEC.md` 스키마 갱신.
- 검증: `scope`(era 제거)·`album`·`staging` 신규 테스트 + `validate.mjs` 자가검증(현 데이터 통과·손상 검출) + 회귀 base 69·w1~w4·room 전부 유지.

## [1.18.0] — 문제 그룹화 모드 (난이도·시대·구성 범위 선택)
출제 범위를 그룹으로 좁혀 모임 성격에 맞게 난이도를 조절.
- **설정 '🎯 문제 범위' 카드:** 축(전체/난이도/시대/구성) 선택 → 값 칩(곡 수 표시)으로 범위 지정.
  - **난이도:** 상(21)·중(21)·신곡(14)
  - **시대:** 데뷔초기 ’15–17(12)·성장기 ’18–20(12)·전성기 ’21–23(18)·최신 ’24–26(14)
  - **구성:** 완전체(단체곡 28)·유닛곡(28)
- **인게임 연동:** 상단 난이도 칩이 곧 난이도 범위 빠른 전환. 현재 범위를 "🎯 범위: … N곡"으로 상시 표시(무대 모드에서 확대).
- **구조:** `config.scope{by,val}` + `songInScope`/`eraBucket`/`scopeCount`로 일원화, `buildOrder`가 범위 필터 적용(범위 0곡이면 전체로 안전 폴백). 저장·지속.
- 검증: 신규 `scope` 15 통과(카운트·축 전환·출제 제한·인게임 전환·지속) + 회귀 base 69·soloauto·quizonly·w1~w4·room 전부 유지.

## [1.17.1] — 힌트 데이터(ALBUM_INFO) 점검·교정
힌트(발매 연도·앨범 종류) 데이터를 웹 검증으로 감사하고 오류를 교정.
- **s10 Imperfect Love** 힌트 메타 누락 보완 → **2021 미니**(Attacca 9번째 미니). 이제 56곡 전곡이 연도/앨범 힌트를 가짐.
- **s45 칠리(Chilli)** 앨범 종류 `미니→정규` 교정(3집 정규 *An Ode* 수록 — 같은 앨범인 Back It Up·Network Love·독:Fear과 정합).
- **s50 거침없이(Just do it)** 앨범 종류 `싱글→디지털 싱글` 교정(BSS 디지털 싱글, 앱의 다른 디지털 싱글 표기와 통일).
- 힌트 생성 로직(초성·제목 마스킹·유닛/단체 분류)은 56곡 전수 점검 결과 이상 없음.
- ⚠️ 미확인(수동 확인 권장): s63 Blue·s64 For you(2025~2026 최신 릴리스, 자동 검증 불가 — 현행 유지).

## [1.17.0] — UX 보완: 결과 화면 홈 버튼 · 자동 진행 기본 20초
- **결과 화면 🏠 처음으로 버튼:** 결과에서 바로 시작 화면으로 복귀(게임 정리+intro 복귀). 게임 화면 홈 로직을 `goHome()`으로 공용화.
- **자동 진행(오토 호스트) 기본 간격 7→20초:** `DEFAULT_CONFIG.autoInterval` 및 설정 입력 기본값, 배포본 `svt-data.json` config(+rev)까지 20초로 통일(정답·MV를 충분히 감상 후 다음).
- 검증: 신규 `misc` 5(자동간격 기본 20·결과 홈 버튼 동작) + 회귀 base 69·soloauto·w1·w3·w4 전부 유지.

## [1.16.0] — W4: 디자인/UX (프로젝터 가독성·무대 모드·게스트 재시도·태블릿·터치) (디자인 페르소나)
프로젝터/원거리·모바일 환경의 사용성을 개선.
- **프로젝터 가독성:** 질문 가사를 `clamp(23→34px)`로 확대(기존 22/모바일 19px가 정답 26·결과 52보다 작던 역전 해소), 카운터·뱃지·타이머 값 상향. 모바일 하한도 19→21px.
- **🎦 무대 모드:** 게임 화면 토글 버튼 신설. 켜면 가사 `clamp(30→52px)`·카운터·타이머를 대형화하고 **속도·가리기·힌트 등 보조 컨트롤을 숨겨** 원거리 관람에 최적화(`body.stage`, `config.stage` 지속).
- **모바일 게스트 연결 안내:** 참가자 화면에 **연결 중/연결됨 상태**를 표시하고, 팀 목록이 ~8초 내 오지 않으면 **실패 안내 + 🔄 다시 시도** 버튼 노출(기존엔 무한 대기·무표시). `makeTransport`의 `onConn` 신호를 활용.
- **태블릿 브레이크포인트:** `561–1024px`에서 4지선다 2열·중간 크기 튜닝(기존 560px 단일 분기).
- **터치 타깃:** 팀 점수 +/- 버튼 30×28 → 40×36(태블릿 44×40)로 확대.
- 검증: 신규 `w4ux` 12 + `w4guest` 5 통과 + 회귀 base 69·soloauto·quizonly·W1·W2·W3·roomtest6·roomauto 전부 유지.

## [1.15.0] — W3: 재미 강화 (효과음·연속정답·축하 애니·최고기록·리캡) (기획 페르소나)
게임 진행의 긴장·보상감을 높이는 재미 요소를 무의존(추가 파일 0)으로 구현.
- **효과음(Web Audio 합성):** 정답 딩·오답 부저·막판 3초 카운트다운 틱·콤보 팡파레. `AudioContext` 지연 생성 + 시작 버튼 제스처에서 unlock(iOS 자동재생 제약 회피). **`효과음` 토글**(기본 ON, 조용한 자리엔 OFF) 게임 설정에 추가.
- **연속 정답(streak):** 솔로에서 정답이 이어지면 카운터에 🔥N 배지, 3·5·7·10·20…에서 "🔥 N연속!" 콤보 알림+팡파레. 오답 시 리셋.
- **정답 축하 애니메이션:** 정답 공개 시 곡명 pop + ✨ 스파클 버스트(가사 박스 위). `prefers-reduced-motion` 존중(모션 최소화 설정 시 비활성).
- **개인 최고기록:** 최고 정답률·최다 연속을 로컬에 누적(통계에 `best`), 결과 화면에 표시하고 갱신 시 "🎉 최고 기록 갱신!".
- **라운드 리캡:** 결과 화면에 "이번에 놓친 곡"(최대 5)을 표시해 복습 유도.
- 검증: 신규 `w3fun` 14 통과(sfx 토글·streak·배지·celebrate·best·recap·무에러) + 회귀 base 69·soloauto·quizonly·autoresult·W1·W2·roomauto·roomtest6 전부 유지.

## [1.14.0] — W2: QA 분석 리포트(로컬 telemetry) (5인 페르소나 리뷰 · QA 미션)
사회자가 모임 후 **어떤 모드가 많이 쓰였고 어떤 곡이 어려운지**를 볼 수 있도록 로컬 통계·리포트 신설(서버 전송·개인정보 수집 없음).
- **통계 수집:** 신규 `svt_lyrics_quiz_stats_v1` 키 — 판마다 `{ts,mode,served,correct,wrong,rate}` 세션(최근 200판) + **모드별 카운트** + **곡별 정답/응답** 적재. 단일 초크포인트 `renderResult()`에 훅, `gameRecorded` 가드로 결과탭 재진입·재시작 중복 방지. 전 모드(솔로/팀전/자동/문제만/온라인·객관식/주관식) 커버.
- **관리자 리포트:** 기존 비번(19950719) 게이트 뒤 "📊 분석 리포트" — 총 판수·전체 정답률, **모드별 이용율·정답률 표**, **곡별 정답률 표(어려운 곡 순, 색상 코딩)**. `↻ 새로고침`.
- **내보내기:** `⬇ JSON`(전체) · `⬇ CSV(곡별)`(엑셀 한글 BOM) · `🗑 초기화`(confirm). 범용 `downloadBlob()` 헬퍼 추가.
- 안정성: `loadStats/saveStats`는 기존 슬롯과 동형 try/catch, 통계 실패가 결과 화면을 막지 않음.
- 검증: 신규 `w2stats` 14 통과(기록·중복방지·모드라벨·곡별집계·리포트렌더·JSON/CSV·200 상한) + 회귀 base 69·soloauto·quizonly·autoresult·W1·roomauto·roomtest6 전부 유지.

## [1.13.0] — W1: 기획의도 복원 + 핵심 버그 수정 + 안정화 + 접근성 (5인 페르소나 리뷰 반영)
5개 페르소나(팀장·QA·개발·기획·디자인) 종합 리뷰에서 도출된 W1 워크스트림 실행:
- **기획의도 복원(기획·디자인 공통 최우선):** `가사 가리기(듣기 전용)`를 **기본 ON**(`config.hideLyrics`)으로 되돌리고 **게임 설정 첫 카드로 노출** — 원래의 "듣기만으로 맞히기" 핵심 명제가 다시 기본 경험. 4지선다·문제만 맞추기에 `변별력↓` 라벨. 진행 중 토글은 설정에 영속.
- **[BUG] 온라인 방 자동 진행 수정:** `autoAfterReveal`/`autoBarVisible` 게이트에 `onlineRoom` 포함 → 정답 공개 후 **자동으로 다음 문항 진행**(+호스트에 ⏸/⏭ 컨트롤 노출). 이전엔 매 문항 후 멈춤.
- **[BUG] 자동진행+팀전+4지선다 채점 수정:** 아무도 안 고른 라운드를 **전부 오답 처리하던 문제** 제거 — 선택이 하나도 없으면 정답만 공개(오점수 방지).
- **안정화(개발):** `saveState`/`persistSlots` try/catch+안내, `fetch(svt-data.json)` **4s 타임아웃**(느린 현장 와이파이 대비), `BroadcastChannel` **feature-detect**(구형 사파리 크래시 방지), 손상 슬롯 로드 방어.
- **접근성(디자인):** 전역 `:focus-visible` 아웃라인, MC 정답/오답에 **✓/✗ 글리프**(색 외 구분 — 색맹·원거리 프로젝터 대응).
- 검증: 회귀 base 69 + soloauto 7 + quizonly 12 + autoresult 2, 신규 W1(가사가리기 기본·auto+team 무오점) 8, 온라인 자동진행 4, roomtest6 11, loadtest 통과.

## [1.12.6] — 기획문서 감사·정합화(문서 vs 코드 vs 데이터)
- 3자 병렬 감사(데이터 정합성 / 문서 vs 코드 / 당초 계획 대비) 후 발견된 지연·불일치 일괄 정정
- **README**: 문제 수 UI를 `10/20/30/전체` → **−/+ 5문제 단위**로 정정(F1), TTS에 문제만 맞추기 예외·**영어 성우 자동 판별** 추가(F4), **문제만 맞추기(quiz-only)**·**개인전 4지선다 자동 채점**·**마지막 문항→자동 결과 이동**·**새 문제로 이어가기** 반영(F5·F6), **오프라인 코어 vs 온라인 방(외부 의존) 고지** 절 추가(P4)
- **OPERATION_GUIDE**: "2초 뒤 자동 낭독"에 문제만 맞추기 예외·영어 성우 한 줄 보강(F3)
- **DATA_SPEC**: config 예시를 실제 기본값(timer 20·mc ON 등 전체 필드)으로 정정(F2·F7), 내장 id `s2~` 정정(F8), 곡목록표 V8 파트 표기 통일(F9)
- **데이터**: s60 "singasong"(순수 영어 가사) part에 `(영어)` 라벨 추가 — index.html·svt-data.json 동기화(rev 갱신)(D1), index.html 소스 주석 `40문항`→`56문항 (상21/중21/신곡14)`(D2)
- 게임 로직 코드 무변경 확인 — 회귀 base 69 + soloauto/quizonly/autoresult 전부 통과, 데이터 56/56 동기화 유지

## [1.12.5] — MV·타임스탬프 갱신 · 마지막 문항 후 자동 결과 이동
- **영상·타임스탬프 커밋**: 운영자 큐레이션 데이터 반영 — Fire·Ruby MV 교체 + 14개 문항 시작 시간 보정, mia 가사 수정(`svt-data.json` rev 갱신, 오프라인 폴백 `index.html` 기본값도 동기화, 배포 config는 기존 유지)
- **마지막 문항 다음 = 자동 결과 화면**: 마지막 문항에서 `다음 →`(또는 채점 후 자동 진행)이면 안내 토스트 대신 **결과 화면으로 자동 이동**(온라인 방은 참가자에게 라운드 종료 안내 후 이동)

## [1.12.4] — 영어 가사 영어 성우 · 문항 전환 시 상단 이동 · 솔로 자동 채점 · 다중접속 부하테스트
- **영어 가사 = 영어 성우**: 가사가 (거의) 영어면 `en-US` 성우로 낭독(한글 음절 비율로 자동 판별). 한국어·혼합(대부분 한글)은 `ko-KR` 유지. 호스트·참가자 폰 공통(`makeUtterance`)
- **문항 전환 시 자동 맨 위로**: 다음 문제로 넘어가면 화면이 부드럽게 상단으로 스크롤(게임 화면일 때만)
- **혼자 플레이 자동 채점**: 솔로+4지선다에서 보기를 고르면 **즉시 정답/오답 자동 집계**(수동 정답/오답 버튼은 숨김) — 4지선다를 끈 솔로만 수동 채점
- **다중 접속 부하 테스트**: 호스트 1 + 참가자 10·20명 동시 접속 시나리오 검증(로스터 집계·상태 수신·전원 제출→자동 공개·채점·폰 공개 반영) 전부 통과

## [1.12.3] — 컬러 팔레트 기능 재편(분류색 ↔ 상태색 직교화)
- 기획의도(프로젝터 미러링·즉각 인지) 검토 → 색이 **두 역할을 겹쳐 쓰던 충돌** 해소
  - 기존 충돌: `상 난이도=오답=타이머위험`(모두 빨강), `중 난이도=선택됨=브랜드`(모두 파랑), `신곡=타이머경고=힌트`(모두 웜)
- **두 직교 계열로 재편**:
  - **분류 계열**(브랜드·난이도·정보) = 로즈 → **라일락**(Very Peri, 팬톤 2022) → 세레니티 스펙트럼 → 난이도 상/중/신곡
  - **상태 계열**(판정·시간) = 신호등 **민트(정답/안전) → 앰버(경고) → 코랄(오답/위험)**
  - **선택됨(picked)** = Peach Fuzz(팬톤 2024) 웜 뉴트럴로 분리(파랑 충돌 제거), **힌트** = 라일락
  - `--lilac/-deep`, `--amber` 토큰 추가
- 색상환에서 분류(핑크·퍼플·블루) ↔ 상태(초록·노랑·빨강)가 반대편이라 프로젝터에서도 구분 명확

## [1.12.2] — 컬러 시스템 재설계(세븐틴 공식색 × 팬톤 트렌드) · 결과 등수표기 · 캐럿 오타
- **컬러 시스템 재설계**(페르소나 토의): 시맨틱 토큰(`:root`)으로 요소별 색 지정
  - 1차 듀오 = **Rose Quartz × Serenity**(세븐틴 공식색·팬톤 2016) — 다크 배경용 채도 조정
  - 웜 하이라이트 = **Peach Fuzz**(팬톤 2024, 기존 골드 대체) · 위험 = **코랄** · 성공/타이머-안전 = 민트
  - 중립(배경·보더)에 **Mocha Mousse**(팬톤 2025) 웜기 미세 주입으로 그라운딩
  - `--rose/-deep · --serene/-deep · --gold/-deep · --green/-deep · --red/-deep · --mocha` 토큰화 → 그라데이션·배지·글로우가 토큰을 추종
- **문제만 맞추기 = 호스트 영상박스·낭독 컨트롤 완전 숨김**: 빈 검은 영상박스 제거(display:none) + 속도/가사 낭독/정지 버튼 숨김
- **결과 등수표기**: 순위에 등수(1위·2위…) + 메달, 동점은 같은 등수(경쟁식 랭킹)
- **오타 수정**: 등급명 '칼럿' → **'캐럿'**(Carat)

## [1.12.1] — 문제만 맞추기 TTS 끔 · 라운드 이어가기 시 참가자 동반 이동 · 5단위 문제 수
- **문제만 맞추기 = TTS도 끔**: `config.quizOnly` 시 호스트 자동 낭독 예약(scheduleAutoTTS) 중단 + 참가자 폰도 자동 낭독 생략(state에 `quizOnly` 전달)
- **라운드 이어가기 시 참가자 동반 이동**(버그 수정): 결과 화면 **새 문제로 이어가기**를 눌러도 참가자만 남고 호스트만 넘어가던 문제 해결. 라운드 번호(`host.round`)를 state/reveal/answer에 실어 방을 **닫지 않고** 새 라운드를 전송 → 접속 중 참가자가 함께 이동. 라운드 종료 시 참가자 폰에 `intermission` 대기 안내
  - 지난 라운드 답안 재전송이 같은 `qid`로 다음 라운드에 새어 자동 공개되던 문제도 라운드 가드로 차단(answer 스냅샷 + 호스트 라운드 대조)
- **문제 수 5단위 선택**: 문제 수를 **−/+ 로 5문제 단위** 조절(또는 전체). 이어가기에서 남은 새 문제가 설정값보다 적으면 **남은 개수만큼만** 출제
- **팀 추가 버튼 위치**: 설정 팀전 카드에서 **+ 팀 추가**를 팀 목록 **상단**으로 이동
- 검증: 2·3-페이지 방 테스트(quizOnly state 전달·영상 미표시·라운드↑에 양쪽 참가자 동반 이동·MC 재표시) + 기존 69항목 통과

## [1.12.0] — 문제만 맞추기 · 새 문제로 이어가기
- **문제만 맞추기**(`config.quizOnly`): 정답 공개 시 **영상 생략**, 답만 표시하고 바로 다음 — 같은 공간에서 폰으로 빠르게 돌릴 때. 온라인 참가자 폰에도 영상 미전송
- **🆕 새 문제로 이어가기**(결과 화면): 지금까지 출제된 문항(playedIds)을 제외하고 새로 뽑아 이어서 진행(점수 유지), 남은 문제 없으면 전체에서 재추첨
- 검증: quizOnly 영상 미표시·새 라운드 중복 0 확인, 기존 68항목 유지

## [1.11.1] — 팀 참가: 드롭다운 선택 + 팀당 1명 독점
- 참가자가 **방 코드 입장 → 팀을 드롭다운에서 선택**(호스트가 정한 팀 목록이 선택지)
- **팀당 1명 독점**: 이미 선택된 팀은 드롭다운에서 비활성 + 동시 선택 시 **서버측에서 후순위 거부**(먼저 고른 사람이 차지)
- 참가 흐름 2단계(코드 입장 → 팀 선택)로 개편, 답·명단·채점을 팀 슬롯(이름) 기준으로 정리
- 검증: 3-페이지 드롭다운·중복 거부·비활성 표시·팀명 채점 통과

## [1.11.0] — 팀명 랜덤 부여 · 참가 QR
- **🎲 랜덤 팀 이름**: 세븐틴 멤버 케미 조합명·라인·유닛 풀(호우주의보·댕냥즈·최씨즈·원정대·부석순·96즈 등, namu.wiki 세븐틴/멤버 간 케미 참조)에서 팀명을 유니크 자동 배정
- **참가 QR**: 방장 로비에 **참가 QR** 표시 — 폰 카메라로 스캔하면 방 코드가 채워진 참가 화면으로 바로 이동(참가 링크도 함께 표시)

## [1.10.1] — 온라인 방 실기기 2차 피드백
- **힌트 순차 공개**: 한꺼번에 노출되던 힌트를 하나씩 공개 — 사람 호스트는 힌트 버튼, 자동 호스트는 **제한시간 비율**(예 20초→경과 5·10·15초)로 자동 공개, 참가자 폰에도 순차 반영
- **정답 MV 자동재생**: 참가자 폰 결과 영상 `autoplay=1`
- **점수 일관화**: 방장 화면 상단·진행 카운터·결과를 방 집계(roster) 단일 기준으로 정렬(정답자 있으면 정답 처리)
- **결과 집계 기준**: 시작 시 지정한 **문제 수(order.length)** 로 전체 문항 표시, 결과 화면에 방 순위 표시
- **참가자 결과 화면**: 게임 종료 시 폰에 "🏁 최종 순위" 표시(자동 종료 포함)
- **재입장 stale 수정**: 방 개설/종료 시 이전 잔여(retained/이벤트) 제거 + 참가자 재입장 시 이전 연결 정리 → 새 코드 입력 시 옛 문항이 뜨던 문제 방지
- **대기 화면 랜덤 MV**: 방장 로비·참가자 대기 화면에서 보유 MV를 랜덤 음소거 재생

## [1.10.0] — 원격(다른 공간) 지원 · 게임 종료/처음으로
- **참가자 폰 자체 완결**(원격 대응): 가사 + **▶ 듣기(TTS)** + 힌트 + **⏱️ 카운트다운** + 보기 + 정답 MV를 폰에서 모두 수신·재생 → 호스트 화면을 못 봐도 진행 가능
- **안정적 원격 전송**: 무료 **Firebase Realtime DB(REST/EventSource)** 전송 추가 — 기본 설정에 DB 주소 입력 시 그 경로로 확실히 동기화, 비우면 공개 릴레이(BroadcastChannel+MQTT) 폴백
- 게임 화면에 **⏹ 게임 종료(결과)** · **🏠 처음으로** 버튼 — 종료 시 참가자 폰에 종료·최종 순위 전송
- 검증: 2-페이지 원격 시나리오 — 듣기 버튼·카운트다운·보기·게임 종료→결과/참가자 종료 표시 통과(기존 스위트 68항목 유지)

## [1.9.1] — 온라인 방 실기기 피드백 반영
- **참가자 대기(로비)** 추가 — 방장이 참가 명단 확인 후 **▶ 게임 시작**으로 시작(자동 시작 방지)
- 참가자 **팀명 전송·표시**(로비/상단바 명단), 답 메시지에도 이름 포함해 join 유실에도 명단 복원
- **전원 제출 즉시 자동 공개·자동 채점** → 다음 문항(타이머 종료 시에도 공개). **방장은 답 선택 불필요**(방장 화면 보기 그리드 숨김)
- 참가자 폰에 **힌트·정답 MV** 표시, 이미 공개된 문항의 중복 state로 화면이 되돌아가던 문제 수정(`revealedQid` 가드)
- **릴레이 연결 상태 표시**(`🌐`/`💻`) — 다른 기기 참가 가능 여부를 방장이 바로 확인
- 검증: 3-페이지(호스트+참가자 2) 로비→시작→각자 제출→전원 제출 시 자동 공개·양팀 채점·정답/MV 전송 통과

## [1.9.0] — 온라인 방(각 팀 폰 참가) · 실험적
- 방장이 방을 열면 **방 코드** 생성 → 각 팀이 폰에서 **🔗 방 참가**로 입장, 문항마다 자기 폰에서 **비공개 답 제출** → 방장 화면에서 **자동 채점·순위 집계**, 결과를 각 폰에 전송(`config.onlineRoom`)
- 전송 계층 분리: **BroadcastChannel**(같은 브라우저·검증됨) + **공개 MQTT 릴레이**(다른 기기·베스트에포트·CDN 지연 로드, 실패 시 자동 폴백)
- 참가자 뷰(코드+팀명 입장 → 답안 패드 → 결과·순위), 방장 상단 방 코드·참가·제출 현황 바, URL `#room=CODE` 바로 참가
- 검증: 2-페이지(호스트+참가자) BroadcastChannel 왕복 — 참가·문항 수신·제출·자동채점·정답 전송 통과. 크로스-기기(공개 릴레이)는 실기기 테스트 필요(샌드박스 CDN 차단으로 미검증)
- 안정적·비공개 운영이 필요하면 이후 Firebase 전송으로 교체 가능(설계 문서화)

## [1.8.0] — 자동 진행(오토 호스트) 모드
- 사람 진행자 없이 게임을 자동 진행하는 **오토 호스트** 추가(`config.autoHost`, 기본 OFF):
  · 상태 머신: 낭독 → 응답 카운트다운 → 자동 정답 공개 → (4지선다) 자동 채점 → 감상 후 자동 다음
  · 진행 컨트롤 **▶/⏸/⏭** (방장/진행 화면용 보조 — 안 눌러도 진행), 마지막 문항 뒤 결과 탭 자동 이동
  · **진행 컨트롤 숨기기**(`autoHideControls`) 토글 → 버튼 없는 키오스크 모드
  · 정답 공개 후 다음까지 간격(`autoInterval`, 기본 7초) 조절
  · 브라우저 자동재생 정책상 첫 시작 탭 1회만 필요(그 뒤 자동)
- 회귀 테스트 추가(무클릭 자동 진행·일시정지·컨트롤 숨김) → Playwright 68항목 통과

## [1.7.0] — 팀 선택 가리기(비공개) 옵션
- 팀전 4지선다에 **팀 선택 가리기** 토글 추가(기본 ON):
  · **온라인·동시 진행**: 모든 팀이 고를 때까지 각 팀 선택을 비공개 → 먼저 고른 팀 선택이 노출되지 않음
  · **오프라인**(팀 논의 후 선택): 끄면 각 팀 선택이 즉시 배지로 표시(운영 방식에 따라 선택)
- 채점 시 전체 공개(팀별 정답/오답 + 정답 보기), `config.teamBlind` 저장·동기화
- 단일 화면 앱 특성상 개별 기기 실시간 동시입력(진짜 멀티플레이)은 미지원임을 문서에 명시
- 회귀 테스트 추가(가리기 ON 중간 비공개 / OFF 즉시 표시) → Playwright 63항목 통과

## [1.6.0] — 팀전 4지선다 팀별 순차 선택·자동 채점
- 팀전 + 4지선다에서 **각 팀이 차례대로 보기를 선택**하는 방식으로 변경(2팀 이상 고려):
  · `🎤 팀X 차례` 표시 + 선택 보기에 팀 배지, 모든 팀 선택 시 **자동 채점**(정답 팀마다 +1점, 타이머 시 시간 보너스)
  · 다음 문항은 **다음 팀부터** 시작해 순번이 공평하게 순환
- **먼저 맞히기**는 개인전/솔로에 한정(4지선다에서 선택 즉시 판정)
- 4지선다를 끈 팀전은 기존 **맞힌 팀 버튼** 수동 채점 유지
- 팀전 스크린샷 갱신, 팀전-4지선다 회귀 테스트 추가 → Playwright 61항목 통과

## [1.5.0] — 2025~2026 유닛 프로젝트 6곡 추가 → 총 56문항
- V8·D&S·CxM·HxW 유닛 앨범에서 공식 MV/영상 보유 곡 6곡 추가(웹서치 4중 병렬 조사):
  · V8(버논X디에잇) 「singasong」·「미아(mia, 버논 솔로)」·「girlsnboys」
  · D&S(도겸X승관) 「Blue」(소야곡 타이틀), CxM 「For you」, HxW 「STUPID IDIOT」
  · 각 곡 제목 미포함 클루 확보(한/영), MV ID는 HYBE LABELS/SEVENTEEN 공식 채널 검증
- "원정대"는 정한X원우(=「어젯밤」)의 팬 조합명으로 확인 → 이미 수록되어 추가 없음
- 8DM(중국어)·b사이드 오디오 온리 트랙·팬 리업로드는 제외
- 상 21 / 중 21 / 신곡 14, 신곡 정의 2024~2026으로 확장, svt-data.json rev 갱신

## [1.4.0] — 저장 슬롯 커밋 일원화 · 발매곡(영어) 3곡 추가
- **저장 슬롯 = 바로 커밋 가능** 구조로 변경(관리자 전용):
  · **+ 현재 상태 저장** 시 슬롯 보관과 동시에 `svt-data.json`을 커밋용으로 자동 다운로드
  · 각 슬롯의 **💾 배포본** 버튼으로 지난 구성도 `rev` 포함 `svt-data.json`으로 내려받기
- 발매곡 3곡 추가(사용자 요청, 영어곡 포함) → **총 50문항** (상 21 / 중 21 / 신곡 8):
  · 우지 「Ruby」(2022), 버논 「Black Eye」(2022), 쿱스X민규 「5, 4, 3 (Pretty woman)」(2025)
  · 파트에 `(영어)` 표기로 사회자가 낭독 특성을 인지하도록 안내

## [1.3.0] — 유닛·솔로곡 7곡 확장 (공식 MV 기준)
- 서브유닛·페어링·솔로 신규 7곡 추가 → **총 47문항** (상 19 / 중 21 / 신곡 7)
  · 부석순 「거침없이(Just do it)」, SVT LEADERS 「CHEERS」, 원우X민규 「Bittersweet」,
    호시 「Spider」, 디에잇 「나란히(Side By Side)」, 도겸 「You're My Christmas」, 호시X우지 「동갑내기(96ers)」
  · 웹서치 3중 병렬 조사로 **한국어 가사 + 공식 MV(HYBE LABELS 채널)** 확인, 제목 미포함 벌스/랩 클루 선정
  · 영어곡(Ruby·Black Eye·CxM 5,4,3)·MV 없는 곡(BSS Lunch)·비공식 리업로드는 제외
- 유닛 가중치 판정(`isUnitSong`)에 LEADERS·솔로·멤버 페어링(X) 인식 추가
- README·데이터 스펙 곡 목록·개수 갱신, svt-data.json rev 갱신(배포 동기화)

## [1.2.0] — 흐름 재구성 · 저장 일원화 · 자동 낭독 · 최종 데이터
- 랜딩 → 기본 설정(게임 옵션) 화면 → 게임 시작 흐름, 저장 기능 저장 슬롯으로 일원화
- 기본 설정 화면 옵션별 안내 문구(토스 톤), 문항 전환 시 2초 뒤 TTS 자동 낭독
- 최종 가사·타임스탬프·영상 링크 반영(40곡 확정), 타이머 20초·4지선다 기본 ON

## [1.1.0] — 문서 보강 · 곡 정리 · 음원 링크 확충
- 미확보 8곡(Trauma·Space·Shhh·박수·어쩌나·거침없이·Spell·Bad Influence) 제거 → **40곡 전곡 가사 입력**
- MV 없는 곡에 **공식 오디오 링크 보강** → 영상/음원 링크 34/40곡 (바람개비·청춘찬가는 공식 MV, Kidult·우리의 새벽·엄지척·Fire는 공식 오디오)
- README 전면 개편(스크린샷·목차·공유 방법·FAQ), 운영 가이드·데이터 스펙 문서 추가
- `docs/img/` 스크린샷 추가(게임·정답 공개·설정·팀전·결과)

## [1.0.0] — 최초 릴리스
- 단일 `index.html` 세븐틴 가사 퀴즈 앱(외부 의존성 0, localStorage 저장)
- 게임: 속도조절 TTS 낭독, 가사 가리기, 정답 공개 + MV 타임스탬프 재생, 수동 채점, 진행률·정답률
- 확장: 난이도 필터, 문항 셔플, 타이머(제한시간·시간보너스), 팀전 모드(점수판·턴 관리)
- 설정: 곡명·파트·난이도·가사·MV·시작시간 편집, 곡 추가/삭제, 일괄 입력, JSON 내보내기/불러오기
- 결과: 정답률·등급 부여, 난이도별 분해, 팀 순위
- 곡 데이터: 실제 가사 발췌를 웹서치로 확보 후 **다수 출처와 교차검증**, 공식 MV 링크·추정 타임스탬프 내장
